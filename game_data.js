//Read data from JSON
const map = read_json("game_data/map.json");
const characters = read_json("game_data/characters.json");
const items = read_json("game_data/items.json");
const inventory = read_json("game_data/inventory.json");


//Reads the content of a json file and returns it's data as object (dictionary)
function read_json(json_path) {

}